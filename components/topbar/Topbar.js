import { Text, View } from 'react-native';
import tw from '../../libs/TailwindConfig';

const Topbar = () => {
  return <View style={styles.container}></View>;
};

const styles = {
  container: tw`bg-gray-black w-full h-15 `
};

export default Topbar;
